#include <stdio.h>
/* Exemplo simples de um modulo 'proximo ao hardware'.
   Aqui poderiamos implementar E/S, leitura de sensores, etc.
   Mantemos uma funcao de exemplo que retorna um codigo. */

int hardware_status() {
    // 0 = ok, 1 = falha
    return 0;
}
